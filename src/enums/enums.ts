export const enum EnumRgb {
    Red = 'Red',
    Blue = 'Blue',
    Green = 'Green',
}


export const enum EnumOperator {
    Plus = '+',
    Minus = '-',
    Division = '/',
    Multiplication = '*',
}